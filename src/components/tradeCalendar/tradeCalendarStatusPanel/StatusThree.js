import React from 'react'

export default function StatusThree({ isOwner, updateBookingStatus, booking }) {
    return (
        <div className="TradeStatusContentContainer">
            <span>booking approved, please get the item ready</span>
        </div>
    )
}
