import { SNACKBAR_BUTTON_TYPES } from "../assets/Data/LBSEnum"

// Auth
export const existEmailandMobile = {
    message: 'Email or Phone Number is already in use',
    btnText: SNACKBAR_BUTTON_TYPES.DISMISS,
    btnFunc: () => {},
}
